# === Secrets / ralph env — real API keys, NEVER commit =====================
.env
.env.deepseek
.env.deepseek-kimi
.env.openrouter
.env.local
.env.*.local
# The example (placeholders only) IS committed:
!.env.deepseek-kimi.example

# === Python ================================================================
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# === Ralph runtime artifacts ===============================================
.ralph-cost.log
.last-branch
archive/

# === Pipeline outputs / media (regenerated; keep out of git) ===============
output/
output_master.mp4
vision_log.json
transcript.json
segments.json
cut_list.json
event_log.json
narration_script.md
frames/
temp/
*.mp4

# === Local model weights (local-only; never commit) ========================
*.safetensors
*.ckpt
*.pt
*.bin
weights/
models/

# === OS / editor junk ======================================================
.DS_Store
Thumbs.db
.aider*
